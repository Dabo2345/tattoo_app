## Summary

Replaces the placeholder `/` page with a complete marketing landing page:

- **Hero** — headline "Arte que dura toda la vida", tagline, CTAs to `/reservar` and `/galeria`
- **Cómo funciona** — numbered 3-step process (Consulta → Diseño → Tatuaje) with Lucide icons
- **Trabajos destacados** — 6-item placeholder grid (real images arrive in #031), link to `/galeria`
- **Artista** — static bio intro with link to `/perfil`
- **CTA final** — closing call-to-action to `/reservar`

All sections use `aria-labelledby` for WCAG AA compliance. Mobile-first responsive layout.
Pure Server Component — no `"use client"`, no API calls (dynamic content comes in #031/#032).

## Tests

8 unit tests in `tests/unit/app/home.test.tsx`:

- H1 rendered
- Hero CTA → `/reservar`
- Hero secondary CTA → `/galeria`
- How-it-works has exactly 3 steps
- Gallery teaser → `/galeria`
- Artist section → `/perfil`
- Final CTA → `/reservar`
- Accessibility: aria-labelled regions present

**Suite: 44 / 44 passed**

## How to test

```bash
npx vitest run tests/unit/app/home.test.tsx
```

Visit `/` in dev to see the rendered page.

## Risks

- Gallery and artist sections are static placeholders — they will be replaced with real data in #031/#032.

## Issue

Closes #030

🤖 Generated with [Claude Code](https://claude.com/claude-code)
