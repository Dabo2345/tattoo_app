## Summary

- **`ConsultationSchema`** — Zod schema for `{ name, email, phone, tattooDescription }` with inline validation messages
- **`BookingWizard`** — `"use client"` 3-step wizard:
  - **Step 1 (date)**: native `<input type="date">` (min = today), next button disabled until date selected
  - **Step 2 (slots)**: fetches `/api/availability?from=...&to=...&type=consultation` on mount via `useEffect`. Shows loading skeleton, empty state, error state, or slot grid
  - **Step 3 (form)**: controlled inputs, Zod `safeParse` validation with per-field error messages, submit → `POST /api/consultations` → `window.location.href = stripeCheckoutUrl`
- **Step indicator**: shows Fecha / Horario / Tus datos with completed (✓) / active / pending states
- **`ReservarPage`**: thin Server Component wrapper

> **Upgrade path**: Step 1 can be replaced with `CalendarPublic` from #029 once merged. Slot grid is functionally equivalent to `SlotPicker` from #029.

## Tests

7 unit tests in `tests/unit/modules/booking/booking-wizard.test.tsx`:

- Renders date step by default
- Next disabled without date
- Next enabled after date selection
- Shows slot step after next
- Shows form step after slot selection
- Validation errors on empty form submit
- Calls `POST /api/consultations` with valid data

**Suite: 43 / 43 passed**

## How to test

```bash
npx vitest run tests/unit/modules/booking/booking-wizard.test.tsx
```

Visit `/reservar` in dev to walk through the full wizard (slots/API will fail gracefully until #016 merges).

## Risks

- Depends on `/api/availability` (#014) and `POST /api/consultations` (#016) and Stripe (#017). Without them, slots show empty state and submit returns an error — both handled gracefully.
- `useState` lazy initializer was mistakenly used for side effects initially; corrected to `useEffect`.

## Issue

Closes #034

🤖 Generated with [Claude Code](https://claude.com/claude-code)
