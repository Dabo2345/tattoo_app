## Summary

- **`GET /api/gallery`** — returns active `GalleryImage[]` ordered by `order ASC`. Optional `?tag=slug` filter via `styleTags: { some: { slug } }`. Response: `{ success, data }`.
- **`GalleryGrid`** — `"use client"` component with masonry columns (2/3/4 cols), style tag filter buttons, and a lightbox (closes on ESC, backdrop click, or close button).
- **`GaleriaPage`** — async Server Component that fetches images + tags from DB directly via `galleryRepository`, passes to `GalleryGrid`.
- **`gallery-repository`** — `findAll(tagSlug?)` and `findAllTags()` wrapping Prisma queries.

## Tests

| File                                               | Tests                                                                                          |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `tests/unit/app/api/gallery.test.ts`               | 3 — 200 with array, tag param forwarded, 500 on DB error                                       |
| `tests/unit/modules/gallery/gallery-grid.test.tsx` | 7 — renders images, empty state, lightbox open/close (button + backdrop), tag filters rendered |

**Suite: 46 / 46 passed**

## How to test

```bash
npx vitest run tests/unit/app/api/gallery.test.ts tests/unit/modules/gallery/gallery-grid.test.tsx
```

Visit `/galeria` in dev (shows empty state if no seed data).

## Risks

- Gallery page calls Prisma directly (needs DB). If DB is not seeded, empty state is shown.
- `<img>` tags used instead of `next/image` — avoids needing to configure Supabase domain in `next.config.ts`. Can be upgraded in #058 (image optimization).

## Issue

Closes #031

🤖 Generated with [Claude Code](https://claude.com/claude-code)
