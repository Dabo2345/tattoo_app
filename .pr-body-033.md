## Summary

- **`StudioInfo` type** — `name`, `tagline`, `description`, `address`, `city`, `phone`, `email`, `workingHours[]`, `parkingInfo`, `accessibilityInfo`, `hygieneInfo`
- **Static data** in `src/modules/content/data/studio-info.ts` (replaced by DB in #044)
- **`GET /api/content/studio`** — returns `{ success: true, data: StudioInfo }`, public endpoint
- **`EstudioPage`** — Server Component with hero, two-column info grid (contact/location + working hours), hygiene section, CTA

## Tests

| File                                        | Tests                                                     |
| ------------------------------------------- | --------------------------------------------------------- |
| `tests/unit/app/api/content-studio.test.ts` | 3 — 200 status, success:true, data matches                |
| `tests/unit/app/estudio.test.tsx`           | 5 — H1 name, address, working hours, CTA, hygiene section |

**Suite: 44 / 44 passed**

## How to test

```bash
npx vitest run tests/unit/app/api/content-studio.test.ts tests/unit/app/estudio.test.tsx
```

## Risks

- Content is hardcoded. Edit `src/modules/content/data/studio-info.ts` until #044 lands.

## Issue

Closes #033

🤖 Generated with [Claude Code](https://claude.com/claude-code)
