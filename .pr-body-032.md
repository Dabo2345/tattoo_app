## Summary

- **`ArtistProfile` type** — `name`, `tagline`, `bio`, `specialties[]`, `experience`, `photoUrl`, `instagram`, `location`
- **Static data** in `src/modules/content/data/artist-profile.ts` — editable constants for the MVP (replaced by DB persistence in #044)
- **`GET /api/content/profile`** — returns `{ success: true, data: ArtistProfile }`, no auth required
- **`PerfilPage`** — async Server Component with hero (photo placeholder + info), bio section, specialties list, dual CTA (Reservar / Ver galería)

## Tests

| File                                         | Tests                                                     |
| -------------------------------------------- | --------------------------------------------------------- |
| `tests/unit/app/api/content-profile.test.ts` | 3 — 200 status, success:true, data matches static profile |
| `tests/unit/app/perfil.test.tsx`             | 5 — H1 name, bio section, all specialties, CTA links      |

**Suite: 44 / 44 passed**

## How to test

```bash
npx vitest run tests/unit/app/api/content-profile.test.ts tests/unit/app/perfil.test.tsx
```

Visit `/perfil` in dev to see the rendered page.

## Risks

- Photo placeholder — no real image until admin can upload one (#044)
- All content is hardcoded. Edit `src/modules/content/data/artist-profile.ts` to change it before #044.
- `Instagram` icon not available in installed lucide-react version — replaced with `AtSign`.

## Issue

Closes #032

🤖 Generated with [Claude Code](https://claude.com/claude-code)
